{
  "version": "1.1.0",
  "description": "Hanaro noise patterns for production-safe parser classification",
  "line_roles_supported": [
    "noise"
  ],
  "classification_policy": {
    "priority": "lowest",
    "description": "noise classification must be applied only after item_detail, item_name, discount_detail, receipt_discount, subtotal, and total checks fail"
  },
  "rules": {
    "header_patterns": {
      "description": "receipt headers and column guide lines",
      "match_mode": "regex_or_contains",
      "patterns": [
        {
          "name": "column_header_full",
          "pattern": "상품\\s*\\(코드\\)\\s*단가\\s*수량\\s*금액",
          "flags": [],
          "description": "하나로마트 컬럼 헤더"
        }
      ],
      "examples": [
        "상품(코드) 단가 수량 금액"
      ]
    },
    "strict_notice_patterns": {
      "description": "거래 데이터가 아닌 안내성/집계성 문구",
      "match_mode": "regex_or_contains",
      "patterns": [
        {
          "name": "event_discount_label_only",
          "pattern": "^이벤트\\s*할인\\s*[:：]?\\s*-?[\\d,.]*$",
          "flags": [],
          "description": "이벤트 할인 설명 또는 집계 라인"
        }
      ],
      "examples": [
        "이벤트 할인: -1,400"
      ]
    }
  },
  "safe_guard_rules": [
    {
      "name": "protect_item_detail_lines",
      "rule": "If line matches any item_detail pattern, do not classify as noise",
      "description": "item_detail must win over noise"
    },
    {
      "name": "protect_item_name_lines",
      "rule": "If line matches item_name pattern or text-like product line fallback, do not classify as noise",
      "description": "valid product name lines must not be swallowed by noise"
    },
    {
      "name": "protect_discount_lines",
      "rule": "If line matches discount_detail or receipt_discount pattern, do not classify as noise",
      "description": "discount lines must win over noise"
    },
    {
      "name": "protect_summary_lines",
      "rule": "If line matches subtotal or total pattern, do not classify as noise",
      "description": "summary lines must be handled by summary_patterns.json"
    },
    {
      "name": "protect_bracketed_name_lines",
      "rule": "If line contains bracket tags but also matches product-name context, do not classify as noise",
      "description": "lines such as [카드쿠폰(율)]선진 삼겹 are not noise"
    }
  ],
  "fallback_policy": {
    "description": "Only assign noise after all item, discount, and summary classifications fail",
    "action": "assign_role=noise"
  },
  "output_policy": {
    "preserve_in_parser_output": true,
    "line_type": "noise",
    "description": "noise lines should remain in parser output for traceability and debugging"
  },
  "parser_notes": [
    "Do not classify text-only lines as noise by default because valid item_name lines may contain no numeric structure.",
    "Do not classify bracket-tag lines as noise before checking item_name and discount patterns.",
    "Do not classify 총구매액, 내실금액, 총할인액, 끝전할인 as noise before checking summary_patterns.json and discount_patterns.json.",
    "Do not use broad heuristics such as 'contains brackets means noise' or 'non-numeric text means noise'."
  ],
  "semantic_notes": [
    "noise lines are not active semantic targets",
    "noise lines should still be preserved for traceability and debugging"
  ]
}